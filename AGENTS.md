# DialogMesh 施工约束（AGENTS.md）

> 定位: 开发流程的"蓝图宏观约束"层 — 按任务类型**激活公理子集**,
> 短上下文中 LLM 才能可靠遵循约束（4-5 条内有效, 25 条全量必然漂移）。
> 公约全文: docs/only/wise/PARADIGM.md（25 公理 + 28 原则）。
> 颗粒度变体表: docs/only/recall/CONTEXT_GRANULARITY_CONTRACT_20260815.md。
> 追踪矩阵: docs/only/wise/PARADIGM_TRACEABILITY.md（施工后更新状态）。

## 铁律（不可协商, 机械检查优先）

1. **原文存在性（P9/A24）**: 任何召回/存储/摘要改动, 原文必须保留
   （full_text / 全文落盘）。摘要与预算只影响注入, 不影响存在。
   验收: 改动涉及截断的, 必须有原文通路（测试断言 full_text 存在）。
2. **记录永不可删（A17/P20）**: 事件链/修改记录不因"干净"而清理。
3. **安全约束不可协商（A21/P24）**: 权限只减不增; 破坏性操作必须
   可回滚。链式命令/越权写入必须被权限门拦截（有测试）。
4. **真实验证优先于指标（A18）**: 任何参数/默认值变更要消融数据;
   自造指标必须真实断言。
5. **双向等价（A24 应用于设计↔实现）**: 改完代码问两句 —
   ①设计→实现: 兑现了吗?（有测试?） ②实现→设计: 这个行为设计
   能解释吗?（解释不了 → 回写设计或修代码, 没有默认漂移）。

## 按任务类型激活的公理子集

### 召回/检索（recall/）
- 激活: P9（信息论分治）A2/P11（颗粒度递归）A15/P18（温度×价值）
  A25/P28（召回是重建上下文）A24/P10（可逆推）A18（真实验证）
- 机械检查: eval_100 回归（检索指标 + 三分 Faithfulness）;
  截断改动对照颗粒度变体表; full_text 存在性测试。
- 禁忌: 不经消融就动融合权重/默认摘要档位; 丢掉原文通路。

### 执行层/蓝图（blueprint/ execution/ event/）
- 激活: A11（可回溯可修正）A16/P19（快反馈后修正）A17/P20（记录）
  A10（元认知监控）A14/P17（约束在事实中）
- 机械检查: 相关单测（executor/statemachine/task_runner）;
  执行迹落树断言; 错误路径不吞异常（显式 degrade）。
- 禁忌: 无约束的自由 ReAct; 工具结果细节压扁（对照变体表）;
  失败路径静默返回空。

### 网关（switch/）
- 激活: A21/P24（护栏）A16/P19（不阻断）A18（真实验证）A14（约束继承）
- 机械检查: 压测/错误码目录/熔断测试; 请求日志; thinking 开关三层。
- 禁忌: 不经消融改超时/熔断阈值; 流式聚合破坏 tool_call 完整性。

### 评测（scripts/ docs/test/）
- 激活: A18（真实验证）A25（检索可追源）A24（可逆推）
- 机械检查: 评测语料不得包含 query 原文（自污染）; 双口径
  （检索指标 + 答案侧三分 Faithfulness）; 基线可复现。
- 禁忌: 改黄金集让数字好看; 口径变更不记录。

### 前端/其他（frontend/ api/）
- 激活: A19/P22（白盒可操作）A16（不阻断）A17（记录）
- 机械检查: tsc 零错误; 接口契约注册。

## 施工流程（每轮）

1. 改前: 读本文件相关任务类型子集 + 对应设计文档（不清楚就问,
   别瞎发明 — 用户明确要求）。
2. 改中: 对照激活的公理; 截断/摘要改动对照颗粒度变体表。
3. 改后: 双向等价核对（铁律 5）+ 相关单测 + 回归。
4. 交接: 更新 PARADIGM_TRACEABILITY 相关行状态; 记录新变体与默认值
   依据; 环境坑补充。
