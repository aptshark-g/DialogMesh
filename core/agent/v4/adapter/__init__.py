"""World Adapter layer.

Contains code/CAD/Unity/DOM adapters. Each adapter may import its own
extractor implementations (tree-sitter, LSP, etc.) -- these dependencies
are isolated within the adapter layer.
"""
