# 元认知仲裁 × 异步介入 — 蓝图=任务地图，执行=复杂网络（2026-08-06）

> 讨论定案：元认知树图 = 微观↔宏观双向纽带（内化仲裁者，非翻译层）。
> 用户介入 = GitHub 更新日志式异步回看（非阻塞），高风险才同步 PlanGate。
> 状态：设计定案，待施工。触发：真 LLM 全链验证通过后，蓝图架构深化讨论。

---

## 一、核心论断（本讨论的定案）

### 1. 蓝图 + 状态机 = 宏观任务地图（防偏离）

```
蓝图 = 地图（路径定义）
  构建 DAG（pcr→intent→context→subgraph→llm_reply）
  策略选择（TEMPLATE / HYBRID / LLM_DRIVEN）

状态机 = 导航（阶段推进 + 仲裁）
  Command→Event→State（GlobalDecider）
  decide() 每次只产生 1 个 Event（防广播风暴）
  每 Tick 检查"该走哪条边"

checkpoint = 偏离检测
  PlanGate: 高风险节点暂停, 用户 approve/adjust/reject
  预算 / 环检测 / 质量门槛
```

### 2. 执行层 = 微观复杂网络关系（树图一体）

```
7 树并行（Discourse/Execution/Constraint/Association/Behavior/Meta/Profile）
  思考时树为核心, 发散/变化时就是图

查询驱动（不是通知）:
  树 A 需要信息 → query → 目标树活跃节点 → 读取
  未找到 → 双方案并行（子 Agent 探索 ∥ 持久化搜索）→ LLM 融合去重

事件流（有环认知）:
  执行产出 → 关联链提炼 → 元认知复盘 → 约束/画像/权重更新
```

### 3. 同一系统的两个投影（不是两个系统）

```
宏观投影: 树/路径/地图（收敛）— 蓝图+状态机 = 可验证主干
微观投影: 图/网络/流（发散）— 7树+事件+关联链 = 复杂网络

宏观 = 缩放投影（A2 颗粒度）: 把网络压成一条主干路径
微观 = 完整网络: 主干每个节点展开都是子网

呼应对话树哲学: "思考树为核心, 发散/变化时是图"
  → 蓝图/状态机 = 宏观的树, 执行层 = 微观的图
```

---

## 二、元认知树图 = 微观↔宏观双向纽带（内化仲裁者）

### 2.1 不是传输/翻译中间层，是内化决策者

```
元认知树图 ≠ 数据搬运（把微观结果转给宏观）
元认知树图 = 仲裁者（读取微观真实状态 → 裁决宏观计划是否要变）

对齐哲学: 元认知 = 统筹/裁决/复盘（不是翻译）
```

### 2.2 双向性（有条件的反向，事件驱动）

```
宏观 → 微观: 蓝图计划指导执行（单向, 已有）
微观 → 宏观: 执行偏差 → 元认知分析 → 裁决改变宏观计划（新增）

反向触发条件（3 信号, 对齐 §十三 自调节）:
  ① 时间偏差（预计超时）
  ② 质量偏差（产出低于基线）
  ③ 用户显式介入（前端反馈）

不是每次执行都反向 — 命中条件才反向, 热路径保持快（A16）
```

### 2.3 执行期策略切换 = RECOVERY 的真语义

```
蓝图 §五 RECOVERY 设计本意: "失败重试→替换子图"（执行期）
审计发现: 现在 RECOVERY 只在构建期约束失败用（P1-22 语义错位）

MC 例子（本讨论）:
  蓝图规划: [pcr]→[intent]→[手搓任务规划]→[执行]→[llm_reply]
  元认知监视: 检测"预计超时"（微观信号）
  → 裁决: 替换规划子图 → [下载 forge → 改造]
  → 前端可见: 用户看到"手搓 → 改用开源 forge"
  → 用户可干预: 允许 / 禁止 / 加约束
```

---

## 三、用户介入 = GitHub 更新日志式异步回看

### 3.1 与主流 agent 的根本区别

```
主流 agent（两种都不好）:
  A. 任务写好一直跑 → 中途想改得手动停
  B. 任务拆很细做一下停一下 → 用户被频繁打断

本方案（变更点驱动）:
  agent 跑, 每个"决策变更点"发一条"更新日志"
  → 用户可回看（git log）
  → 用户可评论/建议（PR review）
  → 用户可否决/回滚（revert）
  → agent 不阻塞（CI 继续跑, PR 挂着审）
```

### 3.2 异步介入的架构本质

```
决策变更 = 事件（写 EventLog, A17）
  kind: strategy_switch / plan_gate / meta_advice / user_correction
  payload: 变更前/后, 原因, 时间, 执行者

回看 = 读事件流（git log 语义）
介入 = 事件流的反向操作
  建议 → 追加评论事件（不打断执行）
  否决 → 触发 revert 事件（回滚）
  约束 → 追加规则事件（后续执行遵守）

执行 = 订阅事件流但不阻塞
  agent 读最新规则/约束 → 下一步执行时生效
  当前步骤不中断（除非用户显式 abort）
```

### 3.3 三层介入分级

| 变更类型 | 介入方式 | 类比 |
|---------|---------|------|
| 低风险决策（策略微调/顺序调整） | **异步日志**, 事后可回看 | CHANGELOG |
| 中风险（元认知建议的策略切换） | **异步 + 通知**, approve/reject | PR review |
| 高风险（写文件/不可逆/花钱） | **同步 PlanGate**, 必须确认 | merge gate |

> 低风险全自动+留痕, 高风险才阻塞 — 防止偏离和不打断同时满足。

---

## 四、资源消耗分层（Hot/Warm/Cold 监视）

```
监视分级（对齐 L5 四区记忆）:
  Hot:   每 Tick 轻量信号（耗时/预算计数器）— 零 LLM, 纯算法
  Warm:  偏差命中 → 单次 LLM 评估（要不要切换）
  Cold:  每 5 轮 → 深度复盘（策略/权重/约束演化）

Rust 重构定位:
  Hot 监视层（计时/计数/阈值）→ Rust, 零开销
  执行引擎（状态机/事件循环）→ Rust, 高并发
  LLM 评估（Warm/Cold）→ Python 保留, 调用密集但逻辑层
```

> 双重认知（执行+监视）资源消耗大是真实代价；
> 优化方向是分层监视（Rust 吃高频低成本, Python 留低频高智能），
> 不是"少做元认知"。

---

## 五、已有资产（无需新建, 只需接线）

| 资产 | 现状 | 缺口 |
|------|------|------|
| EventLog（事件溯源） | ✅ | 决策变更事件 schema 未定 |
| CorrectionJournal（A17） | ✅ 用户编辑日志 | 扩展为 agent 决策日志 |
| 元认知 M4/M5/M8/M9 | ✅ 已接 | check_degradations 无副作用 |
| 关联链 AssociationService | ✅ M→1 已通 | 无 |
| 蓝图 RECOVERY | ⚠️ 构建期兜底 | 执行期策略切换 |
| PlanGate checkpoint | ⚠️ 字段存在 | 执行不暂停 |
| 前端白盒（viz_edit） | ✅ | 变更日志视图 |

---

## 五点五、设计出处核实（2026-08-06 审计 docs/only）

> 施工前审计: 本设计是否已有文档原文？结论 — **大部分不是新发明，
> 是既有设计的接线收敛**。唯一新增 = 异步介入/变更日志。

| 本设计论断 | 设计文档出处 | 状态 |
|-----------|------------|------|
| 蓝图=任务地图/状态机=防偏离 | `blueprint/DESIGN_DEEP_AUDIT` §四（Engine→Decider→PlanGate→Execution 四层） | 设计有, 实现缺 |
| 执行层=复杂网络/树图一体 | `execution/DESIGN_FULL_READ` L100-118（7树+查询驱动+反向回退） | 设计有, 部分实现 |
| 元认知双向仲裁（微观→宏观） | `execution/DESIGN_FULL_READ` L113: "MetaTree 发现冲突/遗漏/环境变化 → 决策节点插入新分支" | 设计原文 |
| RECOVERY=执行期策略切换 | `blueprint/DESIGN_DEEP_AUDIT` §五 L121/127: "RECOVERY 是执行期策略, 语义错位" | 已记录待修 |
| PlanGate 用户介入=人机边界 | `DESIGN_DEEP_AUDIT` L104 + `execution/DESIGN_FULL_READ` L194（PlanGate.create_checkpoint） | 设计有, executor 从不检查 |
| 元认知裁决副作用化 | `BLUEPRINT_AUDIT` P1-10（check_degradations 无副作用）+ `meta/DESIGN_FULL_READ`（复盘引擎 verdict/recommendation） | 已记录待修 |
| **异步介入/变更日志** | **未单独成文**（仅 P0_RETRO "checkpoint/step P1" 字样） | **唯一新增设计** |

### 施工边界确认（与既有待办不冲突, 是收敛）

| 施工项 | 对应既有待办 | 性质 |
|-------|------------|------|
| RECOVERY 执行期切换 | P0_RETRO P1 待办 | 接线 |
| check_degradations 副作用化 | BLUEPRINT_AUDIT P1-10 | 接线 |
| PlanGate checkpoint 接线 | P0_RETRO §7.6 route_mode | 接线 |
| 决策变更事件 schema | 无 | **新增** |
| 前端变更日志视图 | 无 | **新增** |

---

## 六、施工清单（下一步）

### P0（衔接闭环）

1. **决策变更事件 schema**（对齐 EventLog + CorrectionJournal）
   - kind / payload（before/after/reason/actor/ts）
   - 同时服务: 元认知裁决 / 策略切换 / 用户介入
2. **RECOVERY 执行期切换**（executor 支持中途替换子图）
   - 元认知触发 → 替换规划子图 → 重跑
   - 变更写事件流（回看/回滚基础）
3. **check_degradations 副作用化**（M5 延伸）
   - 从"返回 actions" → "真实改 SkillRegistry 权重 + 发事件"

### P1（异步介入）

4. **变更日志视图**（前端, git log + PR review 风格）
   - 回看 / 建议 / 否决 / 约束四操作
5. **三层介入分级生效**（低/中/高风险路由）

### P2（监视分层 + Rust 定位）

6. Hot 监视计数器下沉（为 Rust 化铺路）
7. Warm 单次 LLM 评估触发策略切换

---

## 七、验收门槛

1. MC 场景可复现: 手搓规划 → 元认知检出超时 → 切换 forge → 前端可见
2. 决策变更可回看: 每步"为什么变、谁变的、变成什么"
3. 用户可否决: 切换后 revert 回原计划, 不破坏执行
4. 低风险变更零打断: 异步日志, 无 PlanGate
5. 高风险仍同步: 写文件/不可逆操作必确认
