# P0 复盘：设计 vs 实现对照 — 2026-08-02（压缩前）

> 目的: 压缩前把"设计承诺 ↔ 实现状态"逐项核实，作为交接的唯一对照表。
> 方法: 设计文档（DESIGN_DEEP_AUDIT §7 决策 + DESIGN_BLUEPRINT_ORCHESTRATION 关键节）↔ 本次施工实现 + 测试实测。

---

## 一、§7 决策记录实现对照（7.1-7.7）

| 决策 | 设计承诺 | 实现状态 | 证据 |
------|---------|---------|------|
| 7.1 混合式论证（蓝图=多组合/EDA 契合信息消费/git 式解黑盒） | 文档记录 | ✅ 文档完整 | DESIGN_DEEP_AUDIT §7.1 |
| 7.2 混合式拍板（同步 DAG 段 + 事件广播段 + EventLog） | 同步聚合段直调链组件；异步消费段事件化 | ⚠️ **P0 完成一半**：同步段 ✅（executor 直调 PCR/DualTrack/Subgraph/UnifiedContext）；异步段 ❌（profile/behavior/meta 等仍是 deferred stub，事件广播 P1 做） | executor.py handlers |
| 7.3 关联链/元认知独立服务（防广播风暴） | 定向通道/专用队列，不广播 | ❌ **P1**（executor 的 `_handle_association`/`_handle_meta` 是 deferred 标记） | executor.py |
| 7.4 施工时序（DAG 骨架→EDA→其余消费者） | 先 P0 执行主干再 P1 EDA | ✅ 时序正确（P0 完成，P1 待） | P0_TASK_PLAN |
| 7.5 DAG 快照 = 溯源 + 子图逆向扩展 | 快照持久化 + expand_from_dag_trace | ⚠️ **快照 ✅**（`_save_snapshot` → `data/blueprint_dags/`）；**expand_from_dag_trace ❌ P1**（子图扩展原语待加） | executor.py `_save_snapshot` |
| 7.6 模式空间（route_mode / reply_mode） | 默认值 + 显式覆盖 | ⚠️ **reply_mode ✅**（llm/template 分派）；**route_mode ❌ P1**（checkpoint/step 未实现） | executor.py `_handle_llm_reply` |
| 7.7 统一异步预加载 | 启动后台预热全部模型消费者 | ✅ **完成**（prewarm_models + ModelService 复用单例 + PCR 预热 + discourse_config 修复） | model_service.py / start_engine / bootstrap_v6 |

## 二、P0 任务（T1-T7）实现对照

| 任务 | 设计目标 | 实现 | 测试 |
------|---------|------|------|
| T1 构建层修复 | 单例隔离/strategy 保护/cache 修复/converge 防崩/空串守卫 | ✅ engine.py / llm_dag_builder.py / skill_registry.py | 契约 3 项 |
| T2 执行层混合式 | 统一执行器 + 链组件直调 + 删伪造 + 同 Tick 收敛 + EventLog | ✅ executor.py 重写 + decider.py 瘦身 | 契约 4 项 + 压力 |
| T3 llm_reply 多模式 | mode=llm/template，走网关统一 | ✅ call_switch（127.0.0.1:8080）+ reply_mode 分派 | 契约 2 项 |
| T4 DAG 快照 | 节点摘要 + 边引用持久化 | ✅ `_save_snapshot` | 契约 1 项 |
| T5 生产接线 | v3_session_api 真实 intent + 新执行器 | ✅ Phase 3.5/5 改 DualTrack intent + executor.execute | —（API 需 Hermes 验证） |
| T6 契约测试 | 对抗性真断言 | ✅ tests/test_blueprint_v2.py（10 项，0.9s） | 全绿 |
| T7 回归 + 深度 | 不破坏现有 + 加压 + 监控 | ✅ tests/test_blueprint_v2_stress.py（16 项，slow） | 默认 59p/4x |

## 三、DESIGN_BLUEPRINT_ORCHESTRATION 关键承诺对照

| 设计承诺 | 实现状态 |
---------|---------|
| §一 建图/跑图分离 | ✅ 构建层（engine/skill_registry/llm_dag_builder）完成 |
| §四 四层架构（Engine→Decider→PlanGate→Execution） | ⚠️ Engine✅ Decider✅（瘦身委托） PlanGate❌ Execution❌（P1/P2） |
| §六 最小闭环（选蓝图→建图→执行→审核） | ⚠️ 前三步✅，审核❌（P1 PlanGate） |
| §十 Level1/2/3 | ⚠️ Level1✅ Level2⚠️（模板+构建期 override，checkpoint/step P1） Level3⚠️（有 diverge/learn/converge，四保护缺三） |
| §十一 LLM_DRIVEN 四保护 | ⚠️ Budget✅ PlanGate/LoopDetector/QualityGate❌ |
| §十四.3 EventBus 订阅表 | ❌ P4 架构目标（P0 用混合式） |
| §十四.4 Meta 回写 | ❌ P1（MetaFeedback 死代码待接） |

## 四、核实结论

1. **P0 目标全部达成**：构建→执行真连通、无伪造数据、同 Tick 拓扑、llm_reply 走网关、快照/监控/预热落地、测试全绿（59p/4x 默认 + 16 压力）。
2. **设计决策 7.2/7.5/7.6 的"异步段/溯源扩展/route_mode"是 P1 明确待办**——不是遗漏，是施工时序（7.4）的安排。
3. **P1 待办清单**（下一阶段）:
   - PlanGate 暂停 + CorrectionJournal（§7.6 截断点基础）
   - Meta 闭环（MetaFeedback 消费 EventLog + 权重副作用 + 权重公式 base 修复）
   - 关联链/元认知 EDA 独立服务（§7.3）
   - expand_from_dag_trace（子图第三种扩展原语，§7.5）
   - route_mode checkpoint/step（§7.6）
   - PCR 模型统一（bge-small-zh-v1.5 → SemanticEncoder/ModelService，消除 fastembed 重试）
   - API 层 Hermes 3.11 端到端验证（v3_session_api 需 fastapi）
   - CLI `dm blueprint/decider` 假命令修对象（连 GlobalDecider/StateMachine 而非蓝图 Decider）
4. **非本会话未知改动**（工作区存在，未触碰）: `.codegraph/`、frontend/gateway/persistence_rs、`.gitignore`、`config/mood_profiles.yaml`、`association/semantic_coref.py`、`cli/commands/batch4_cmd.py`+`pcr_intent_cmd.py`、`api/v6_app.py`、`docs/DESIGN_LEARNING_INGESTION.md`——压缩后需确认来源。
