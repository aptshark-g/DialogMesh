# DialogMesh UI 重构实施计划(在现有基础上渐进改,不重写)

> 2026-08-15 立项 · 依据:UI_DESIGN_DISCUSSION.md(方向 4 纸上工作室)、UI_DETAIL_GAP.md(P0–P1 细节清单)、RIGHT_SURFACE_MODEL.md(双槽位)、PROJECT_DIMENSION.md(项目组)
> 原则:**渐进修改现有代码**,每步 `tsc --noEmit` + `npm run build` + Playwright 截图验证;**本轮全部不动后端**;浅色模式(light.css / :root.light)同步维护。

## 0. 现状资产盘点(已核实)

| 资产 | 现状 | 结论 |
|---|---|---|
| Design tokens | `src/index.css` 已有双套(`:root` 暗 / `:root.light` 亮),Tailwind 映射 | 沿用其色值家族,只**增补**不发色 |
| 滚动条 | 已定制但常驻可见(6px,`--border-medium`) | 改 overlay 式(默认隐藏,hover 淡入) |
| Sidebar | `Sidebar.tsx` 平铺 13 项,lucide 图标,framer-motion,有 collapsed/mobile 态 | 分组改造 + 项目组插入 |
| 右栏 | `RightPanel.tsx` + `components/dock/`(已有 DockPicker!) | 先浮动卡片化,后改上下文工作台 |
| 双槽位 | 无,Layout 固定结构 | 需新建布局 store + 表面注册表 |
| 项目概念 | 前后端均无(GUI_API v10 已 grep 证实) | 前端-only 假数据起步 |

## 1. 分期路线

| 阶段 | 内容 | 改动面 | 状态 |
|---|---|---|---|
| P0-A | token 增补(hairline/radius/dock 底色)+ 滚动条 overlay 化 | index.css / light.css | ✅ 本轮完成 |
| P0-B | Sidebar 分组(主导航/项目/洞察/工程/系统)+ 双激活态区分 + 底部瘦身(悬浮/嵌入移走,Backend 状态点化) | Sidebar.tsx | ✅ 已完成(08-16, 截图 real_p0b_chat.png) |
| P0-C | 分隔重做:砍通长分割线,dock 改浮动卡片(栏间缝 + 圆角 14px);附带修掉 dock 双标题头(DockPanel 内层头移除) | SidePanel.tsx, RightDock.tsx, DockContents.tsx, Toolbar.tsx, tailwind.config.js(语义类), light.css(dock 例外) | ✅ 已完成(08-16, 截图 real_p0c_chat_{dark,light}.png) |
| P0-D | 去示范数据 + 比例调整:项目组空态/画像假数值(雷达 84/76…、指标卡 76/84/71、状态条 82%/18%)全部改诚实空态;空态判定看 oceAN_dims 本身(后端首用返回空对象,非全 50);MetricCards 趋势 ↑0 假精度隐藏;聊天列与输入条 max-w-3xl 居中,输入条改浮动圆角条;dock 默认宽 340→320 | Sidebar.tsx, MetricCards.tsx, StatusProgress.tsx, CognitiveRadarChart.tsx, DockContents.tsx, ChatPanel.tsx, ChatInput.tsx, uiStore.ts | ✅ 已完成(08-16, 截图 real_p0d_{chat_dark,chat_light,final_dark}.png) |
| P1-A | 双槽位骨架:layoutStore(配对记忆)+ 表面注册表(SURFACES/SURFACE_MAP/交换判定)+ 副槽切换器/交换⇄ + ChatSideSurface(副槽迷你对话, useChatSend 与主槽共享);顺带修正 auto 配对死映射(/task-planning→/tasks) | 新 lib/{layoutStore.ts,surfaceRegistry.tsx}, components/dock/ChatSideSurface.tsx, hooks/useChatSend.ts, RightDock/CenterDock/dockTabs/uiStore/ChatPage | ✅ 已完成(08-16, 截图 real_p1a_{switcher,chat_side,swapped}.png) |
| P1-B | 聊天页副槽 = 上下文工作台(本轮注入条:域分布/token 占比;记忆卡三态:正常/钉住/移除,本地状态+全部恢复;8s 静默轮询;/chat 默认配对改为 context) | 新 stores/contextWorkbenchStore.ts(指纹键+三态), DockContents.tsx(ContextDockContent 重写), surfaceRegistry.tsx(title+默认配对) | ✅ 已完成(08-16, 截图 real_p1b_{workbench,marks,empty}.png) |
| P1-C | 顶栏瘦身+万能搜索栏+工作台模式:搜索框(纯装饰,从未接线) → ✨万能搜索栏触发器(搜索·提问·代操作 + ⌘K chip);迷你面板真实可用(13 页面跳转+主题/副槽操作,↑↓/Enter/Esc,内容搜索与代操作留位 B13/B14);画像监控 → 状态点(绿活跃 n 维/灰冷启动/红离线,15s 轻轮询,点击进画像页,B4 临时方案);上下文工作台加模式切换(列表 / 图结构·B11 / 精调·B12,后两者诚实占位不放假数据);悬浮/嵌入挪入坞头(副槽⇄浮层互转按钮),DockPicker 退出侧栏;顺手修 Esc 穿透 bug(面板 Esc 曾连带关坞,palette 内 stopPropagation) | Toolbar.tsx 重写, 新 stores/omniboxStore.ts + components/omnibox/OmniboxPalette.tsx, DockContents(模式切换+ModePending), RightDock/CenterDock(placement 按钮), Layout(挂面板), Sidebar(去 DockPicker) | ✅ 已完成(08-16, 截图 real_p1c_{toolbar,omnibox,mode_graph,mode_finetune,float}.png) |
| P1-D | 比例对齐 mockup v2(去"幼"感):侧栏 256→228px/nav 13px 纵向 6px/图标 15px/新会话药丸;顶栏 48→52px/标题 15px/触发器药丸 12px/幽灵钮 36→30px;聊天列 768→720px/输入条全药丸/发送钮 48→32px;副槽切换器药丸化(wash 底)/卡片统一 10px 圆角/钉住整圈琥珀边改 inset 2px 左条(mockup 语义);FAB 56px 琥珀球→40px 中性(第四对话入口,是否移除待拍板);新增 wash/wash-strong 双主题 token(修掉 card-hover 在 light.css 覆盖选择器下被误排除的浅色 bug) | Sidebar/Toolbar/ChatPanel/ChatInput/RightDock/DockContents/index.css/tailwind.config/FloatingActionButton | ✅ 已完成(08-16, 截图 real_p1d_{dark,light}.png) |
| P1-E | 质感基调:① 根治"白线分隔" — 根因是 Tailwind 静态 gray 阶(border-gray-200 等,#E5E7EB)在暗色下保持亮灰,新建 dark.css(light.css 镜像)把静态 gray 描边/divide/底色/hover 全局重映射到 --border-hairline/--bg-wash 等 token,保留 :focus 焦点描边;② 工作台卡片去描边走 wash 灰度阶梯(mockup v2 语义,按钮/输入框保留描边 affordance);③ 浮层玻璃化:omnibox/切换器下拉/悬浮坞 → --bg-glass + backdrop-blur-xl + saturate-150 + --shadow-float(苹果式:基底平直,质感只给浮层);④ 触发器文案直白化:搜索·提问·代操作→搜索,⌘K→Ctrl K(Windows 用户看不懂 ⌘);⑤ 侧栏激活/hover 走 wash,新会话改实心琥珀主按钮(mockup .btn-primary 语义) | dark.css(新)/index.css/tailwind.config/Toolbar/OmniboxPalette/RightDock/CenterDock/DockContents/Sidebar/main.tsx | ✅ 已完成(08-16, 截图 real_p1e_{dark_main,dark_omnibox,light_main,light_omnibox,dark_main2,dark_chat,dark_eng}.png) |
| P2 | 项目组接数据:projectStore(localStorage `dm_projects`,zustand persist):projects(id/name/color/createdAt,六色板循环)+ sessionProject 映射 + activeProjectId;侧栏项目组可建(+ 内联输入 Enter/Esc)可选(激活=过滤范围并跳 /sessions,再点取消);会话页过滤 chip/过滤空态/行内归入按钮(常显 FolderPlus,玻璃下拉)/归属色点;行容器 button→div 合法化嵌套;顺带修顶栏触发器白线(border-subtle→hairline+bg-wash);P2b 项目管理:项目项右键/hover「⋯」开玻璃菜单 — 重命名(内联输入)/改色(六色板,PROJECT_PALETTE 导出+recolorProject)/删除(归属映射自动清除),click-away 遮罩 | stores/projectStore.ts(新)/Sidebar/SessionsPage/Toolbar | ✅ 已完成(08-16, 截图 real_p2_{toolbar,empty,dropdown,filtered,menu,rename,renamed}.png) |
| P1-F | 白线根因根治 + 立体感:① 命名缺口 — config 的 colors.border.* 嵌套只生成 border-border-*,全站 201 处 border-subtle/medium/hairline 从未被生成,回落 preflight 默认 #E5E7EB;在 index.css @layer utilities 补真工具类(走双主题 CSS 变量,同层 hover:/focus: 变体特异性更高故交互态安全),构建产物已验证 .border-subtle 存在;② 立体感(苹果式上方打光):dark.css 给 shadow-card 加 inset 0 1px 0 rgba(255,255,255,.045) 内高光,body 加 1100px 径向环境光(白 2.8%);③ 侧栏滑动 hover 药丸:framer-motion layoutId="nav-hover-pill" 共享药丸在页面项/项目项间弹簧滑动(stiffness 500/damping 40),替换原 hover:bg-wash 瞬时跳变,离开 nav 清空 | index.css/dark.css/Sidebar | ✅ 已完成(08-16, 截图 real_p1f_{dashboard,hoverpill,sessions}.png) |
| P3 | 会话切换器(顶栏「DialogMesh ⌄」死按钮激活)+ 路由驱动会话:① chatStore 加 loadSession(id,msgs);② 路由 chat → chat/:sessionId?;③ ChatPage 路由驱动 — /chat=恢复或新建、/chat/new=强制新会话(建后 replace 回 /chat)、/chat/:id=getHistory 真载入历史(失败降级空会话允许进入);④ 修 /chat/default 死链(原命中 NotFound):Sidebar 新会话→/chat/new、MobileBottomNav 聊天→/chat、SessionsPage 新建→/chat/new、DashboardPage 新建会话主按钮→/chat/new(同文件第二处 navigate 用后续 className 锚点区分);⑤ 新 components/SessionSwitcher.tsx:过滤框 +「+ 新会话」+ 按项目分组(项目色点/计数/当前✓琥珀高亮)+ 未分配组,玻璃质感,Esc/点外关闭,打开时 refresh;顶栏标题在聊天页显示「会话 <短id>」+ 项目色点;⌘K 面板已于 P1-C 先行完成 | chatStore/App/ChatPage/Sidebar/MobileBottomNav/SessionsPage/DashboardPage/Toolbar/SessionSwitcher(新) | ✅ 已完成(08-16, 截图 real_p3_{switcher,switched}.png) |
| P1-G | 全局玻璃质感(macOS 式):① 新 token --bg-glass-strong(面板级,更高不透明度保可读)接入 tailwind;② 顶栏改 absolute 玻璃浮层(Layout 重构:滚动容器 pt-[52px],全页面内容从栏下滚过;仅覆中栏宽度,右坞保持全高浮卡);③ 聊天输入条改 absolute 玻璃横条(消息流 pb-28 从其下滚过,Apple Messages 式);④ 左侧栏 bg-surface-sidebar → 玻璃(透出 body 环境光,移动抽屉真模糊)+ 右缘 hairline;⑤ 右坞浮卡 bg-surface-dock → glass-strong;原则:backdrop-blur 只对浮于内容之上的层生效,内联按钮保持实色 | index.css/tailwind.config/Layout/Toolbar/ChatPanel/Sidebar/SidePanel | ✅ 已完成(08-16, 截图 real_p1g_{chat,dashboard}.png) |
| P1-H | 液体玻璃(Liquid Glass 近似)+ scrim 根治 + 侧栏拖拽 + 分隔光影:① scrim 根因 — light.css `[class*="bg-surface"]` 通配把 bg-surface-overlay 刷成不透明 #FDFCF8(特异性压过第 20 行的修复意图),新建独立 `bg-scrim` token 工具类绕开通配,OmniboxPalette/Modal/SessionDetailDrawer 三处遮罩全换;② 新材质 `.glass-panel`(padding-box/border-box 双背景层渐变 rim light + 顶部内高光 + 底部内阴影厚度感,token 双主题),五处浮层全换(omnibox/会话切换器/侧栏项目菜单/会话页归入下拉/右坞切换器下拉);③ 侧栏指针镜面高光(sidebar-spec::before 径向渐变跟 --mx/--my,hover 淡入);④ 左侧栏可拖拽调宽(uiStore `dm_sidebar_width` 持久化,180-320,拖拽中禁过渡,收起时隐藏手柄)+ 右缘渐变发丝线 edge-fade-r(顶强下隐);⑤ 结构条软投影 shadow-bar-b/t(顶栏/输入条);CenterDock 保持原样未换;真折射(SVG filter/shader)明确不做 | index.css/tailwind.config/uiStore/Sidebar/OmniboxPalette/Modal/SessionDetailDrawer/SessionSwitcher/SessionsPage/RightDock/Toolbar/ChatPanel | ✅ 已完成(08-16, 截图 real_p1h_{omnibox_dark,omnibox_light,sidebar_spec}.png) |
| P1-I | 高光约束进按钮 + 剩余玻璃面全换装:① 侧栏级大光晕 sidebar-spec 废弃 → `.spec-item` 逐按钮 spotlight(Linear/Vercel spotlight card 同款:overflow 裁剪圆角,::before 110px 径向渐变跟 item 本地 --mx/--my,仅小面积重绘;--spec-color 暗色 0.07→0.10),页面导航项 NavLink 与项目项 button 各接 onPointerMove;② 新增 `.glass-panel-strong`(玻璃强基底变体),右坞浮卡换装;CenterDock 浮坞换 glass-panel(保留 border-l);③ shadow-bar-b/t 叠加 inset 顶部内高光(结构条也有上方打光);调研结论:真液体折射=SVG feDisplacementMap/feSpecularLighting,有性能与可访问性成本,业界建议只用于高曝光小组件 — 维持不做;高光高性能做法=CSS 变量驱动渐变(避免 transform 层方案的多 DOM) | index.css/Sidebar/CenterDock/SidePanel | ✅ 已完成(08-16, 截图 real_p1i_{spec_item,dashboard,light}.png) |
| P1-J | 药丸滑动回归修复 + 浮层追光 + 页面卡片液体化试点:① 回归根因 — P1-I 给 .spec-item 加的 overflow:hidden 裁掉了 layoutId 药丸的飞行过程(药丸 transform 经过新父级裁剪区时不可见,只剩落点"冒出来");修复 = 移除 overflow,改 ::before border-radius:inherit(辉光自身裁圆角),药丸飞行全程可见(飞行中间帧截图实证);② 新 lib/spec.ts specMove 共享 handler + `.spec-panel` 浮层追光(320px 大半径低透明 sheen,光在玻璃表面流过),omnibox/会话切换器/右坞下拉/会话页下拉/侧栏项目菜单五处接入;③ `.card-liquid` 页面级液体卡片(克制 rim 渐隐 + 半透明基底透环境光,无 backdrop-filter 省性能),总览页 6 张卡试点(统计卡/图表卡/列表卡),配 shadow-card | index.css/lib/spec.ts(新)/OmniboxPalette/SessionSwitcher/RightDock/SessionsPage/Sidebar/DashboardPage | ✅ 已完成(08-16, 截图 real_p1j_{pill_flight,pill_landed,cards_dark,omnibox_sheen,cards_light}.png) |
| P1-K | 亮色光圈调色 + 页面粘边修复 + 设计语言落档:① 调研(uxpilot 等)亮色玻璃质感靠色彩/边缘近白 rim/柔和阴影,白光斑在浅底无对比 — 亮色 --spec-color/--spec-panel-color 换品牌琥珀 rgba(217,119,6) 0.14/0.08;② SessionsPage/SettingsPage 根容器加 px-6 lg:px-10 pb-10(max-w-5xl 居中但零横向 padding,视口收窄时内容粘左右边界);③ 设计语言沉淀为 docs/only/frontend/DESIGN.md(五原则/材质层级表/双主题规则/交互规范/禁区教训/文件地图) | index.css/SessionsPage/SettingsPage/DESIGN.md(新) | ✅ 已完成(08-16, 截图 real_p1k_{spec_light,omnibox_light,sessions}.png) |
| P1-L | 右坞切换器滑动药丸 + 侧栏药丸提速 + card-liquid 全站铺开:① 右坞表面切换器下拉加 layoutId="dock-switcher-pill" 滑动药丸(新 state switcherHover,容器 onMouseLeave 清空,选项 onMouseEnter+relative+去瞬时 hover 底色,spring 700/45 比侧栏更敏捷);② 侧栏药丸提速 stiffness 500/damping 40 → 680/45(Sidebar 两处:页面项+项目项,"平滑移动稍微快一点跟进鼠标");③ card-liquid 铺开 9 页 60 张卡:CognitiveProfile×3/Behavior×5/DeepChain×7/Engineering×4/Gateway×16/MetaCenter×15/Graph×1/Sessions×1/Settings×8,六条替换规则长模式优先,Graph 页 rounded-md 分段控件有意保留 | RightDock/Sidebar/index.css+9 页 | ✅ 已完成(08-16, 截图 real_p1l_{switcher_flight,switcher,engineering,meta}.png) |
| P1-M | card-liquid 指针追光 + 浅色铺开页抽查:① 用户提出"页面内部鼠标发光+光圈按玻璃大小调整":card-liquid::before 加 radial-gradient(ellipse 42% 42% at --mx/--my) — 百分比半径随卡片尺寸自适应(大卡大光晕/小卡收敛,无需尺寸分档),--card-spec-color 双主题(暗 rgba(255,255,255,0.06)/亮 rgba(217,119,6,0.10) 琥珀),::before 叠内容之下透光不遮字,card-liquid 补 position:relative;② main.tsx 全局 pointermove 委托:单监听器 closest('.card-liquid') 写 --mx/--my(passive),60 张卡零 JSX 侵入且覆盖未来卡片,与 spec-item/spec-panel 手挂 specMove 共存;③ 浅色铺开页抽查:元认知/工程/网关浅色全过无白线,琥珀追光浅底成立 | index.css/main.tsx | ✅ 已完成(08-16, 截图 p1m_{meta_spec_small_dark,meta_spec_big_dark,meta_spec_light,engineering_light,gateway_light}.png) |
| P1-N | 边缘 rim 追光(玻璃感修正):① 用户反馈 P1-M ellipse 大光晕"完全没有玻璃感,光到不了边缘" — 调研业界共识(Win10 Fluent 照亮边框/Cruip spotlight card 1px 缝透光/antd BorderBeam mask 环带/Aceternity moving border): 玻璃追光的主角是边缘不是面板;② card-liquid::after 新增 rim 追光: padding 1.5px 撑环带 + mask-composite exclude 差集裁边缘环, radial-gradient 240px circle at --mx/--my, 鼠标近哪段边缘哪段亮(物理: 光透过玻璃照亮磨砂倒角), --card-rim-glow 双主题(暗 0.50 白/亮 0.45 琥珀);③ ::before 中央光降级辅助: ellipse 42% → 280px circle, --card-spec-color 减淡(暗 0.06→0.04/亮 0.10→0.07)防雾面 | index.css | ✅ 已完成(08-16, 截图 p1n_{meta_center_dark,meta_edge_dark,meta_edge_light,sessions_zoom_dark}.png) |
| P1-O | rim 边缘追光风格统一 + 会话/设置页顶距:① 用户确认 P1-N 效果并拍板统一: spec-item::after 加 130px rim 环带(小按钮边缘几乎全程点亮,侧栏 hover 整圈描边,还原"光到达边缘"质感), spec-panel::after 加 320px rim 环带(浮层边缘流光), 复用 --card-rim-glow + mask 差集双写兜底;② 会话页"顶太高/太接近顶上": SessionsPage/SettingsPage 根容器缺 pt(P1-K 只加了 px/pb), 补 pt-6 对齐 Dashboard py-6 基准; ChatPage 全高消息流布局不动;③ DESIGN.md 升级 v2: 材质表/禁区§5(mask 双写)/文件地图/后续方向全量同步 | index.css/SessionsPage/SettingsPage/DESIGN.md | ✅ 已完成(08-16, 截图 p1o_{sidebar_rim_dark,omnibox_rim_dark,sessions_top_dark}.png) |
| P4 | 浅色模式逐页验收;移动端抽屉化 | 全量 | 待做 |

## 2. 后端调整需求登记表(开发中持续追加,本轮不动后端)

| # | 需求 | 触发场景 | 前端临时方案 | 状态 |
|---|---|---|---|---|
| B1 | `project_id` 实体:session/task/graph-node 挂项目字段 | 项目组切换要真实过滤 | localStorage 映射表 session_id→project | 登记 |
| B2 | ContextCompiler 检索加 `project` 范围参数 | 项目 = 认知边界的核心价值 | 无(等 B1) | 登记 |
| B3 | 上下文钉住/移除接口(记忆片段级,作用于下轮编译) | 上下文工作台的记忆卡三态 | 本地状态,仅 UI 表达 | 登记 |
| B4 | 画像健康度聚合值(一个数)供顶栏状态点 | 监控数据撤出右栏后的归宿 | 复用现有画像端点取首值 | 登记 |
| B5 | 会话标题摘要 + 相对时间字段(替代裸 session id) | 会话列表人性化 | 前端截断 + 本地别名 | 登记 |
| B6 | 画像"成功状态/风险状态"指标数据源(原 StatusProgress 82%/18% 是纯装饰,从未接线;P0-D 起前端隐藏该卡) | 右栏画像/画像页要展示健康度与风险 | 组件无数据时显示空态,dock 内暂不渲染 | 登记 |
| B7 | 画像冷启动语义决策:`turn_count=0` 时 `/v6/profile` 返回的 `oceAN_dims` 是空对象(实测 08-16),不是全 0.5 基线 — 产品上要"空态"还是"全 50 基线起步"?若要基线,后端需在首用时返回基线值 | 首次使用的画像呈现(用户明确要求"默认正常即可") | 前端对空 dims 统一显示空态(hasDims 判定) | 待后端决策 |
| B8 | 槽位配对偏好持久化:配对记忆现存 localStorage(`dm_layout_pairing`),跨设备同步需用户偏好端点 | P1-A 副槽配对记忆 | localStorage | 登记 |
| B9 | 自动化视口配对协议(远期):UI 自动化时主槽=执行视口/副槽=对话,需要自动化运行时的视口状态流(截图帧/CDP 等),纯前端无法做 | 用户提出的"中间屏幕像虚拟机"场景 | 无 | 远期 |
| B10 | `/v6/context` 字段补全:① `total_tokens`/`budget` 未返回(注入条只能显示已用分布,无法显示预算水位);② 条目无稳定 ID(钉住/移除的本地键只能用内容指纹,条目内容微调后标记即失配) | P1-B 上下文工作台 | 前端用 estimated_tokens 求和代替总量;内容散列做会话内指纹键 | 登记 |
| B11 | 上下文条目 ↔ 图谱节点映射 + 图结构选择接口:按图谱结构圈选注入范围(节点即记忆,沿边扩散),需要条目携带来源 node_id 及"选中子图作用于下轮编译"的写接口 | 上下文工作台·图结构模式(用户提出,区别于线性列表挑选) | 模式页为"接口未就绪"诚实占位,列表模式照常可用 | 登记 |
| B12 | 最终注入上下文编译快照:查看送入 LLM 的最终分段(system/记忆/工具结果等),逐段增删改后生效 — 需要编译快照读接口 + 分段级覆写写接口 | 上下文工作台·精调模式(用户提出,"对最终供 LLM 的实际上下文精调") | 同上,诚实占位 | 登记 |
| B13 | 全局内容搜索端点:跨会话历史/上下文条目/图谱节点的统一检索(供万能搜索栏) | 顶栏万能搜索栏(用户:找上下文内容、找之前的对话) | 迷你面板先做真实页面跳转+操作,内容搜索留位 | 登记 |
| B14 | 元认知代操作协议:自然语言指令 → 元认知规划 → GUI/系统操作(经 checkpoint 审批),供万能搜索栏"帮我操作"用法 | 万能搜索栏"让元认知帮忙操作"(用户提出) | 无 | 登记 |
| B15 | 项目 CRUD + 会话归属的服务端持久化:P2 前端已落地 localStorage 版(存 `dm_projects`:projects=id/name/color/createdAt,sessionProject=sessionName→projectId);需要 GET/POST/PATCH/DELETE `/v6/projects` + 归属写接口,会话列表项返回 `project_id`;上线迁移时以前端本地数据做初始导入 | P2 项目组(侧栏可建可选、会话页过滤 chip/行内归入) | localStorage 全量实现,已可用 | 登记 |
| B16 | 新建会话携带 `project_id`:激活项目下点"新会话"应自动归入该项目(前端暂做不了:会话名由后端生成且时机晚于路由跳转) | 项目内工作流闭环 | 无(等端点,前端预留 activeProjectId 状态) | 登记 |

## 3. 风险与约束

1. **扩展模式**(`.extension-mode`,360px 宽浏览器插件壳)别破坏——Sidebar 改动要过一遍该模式
2. **MobileBottomNav** 保留;P4 才处理移动端深化
3. 不改路由路径,不删页面;任务页等"待决命运"页面保持原样直到拍板
4. 每步提交前:`npx tsc --noEmit -p tsconfig.app.json` + `npm run build` 全绿才算完
5. git:仓库有并行会话扫提交的先例,改动前确认分支与工作区状态

## 4. 变更日志

- 2026-08-15 P0-A 完成:index.css 增补 `--border-hairline` / `--r-card` / `--r-pill` / `--bg-dock` 四枚 token(双主题),滚动条改 overlay 式(默认隐藏,hover 出现,8px 全圆角)
- 2026-08-16 P0-B 完成:Sidebar 分组(主导航/项目/洞察/工程/系统)+ 双激活态区分 + 底部瘦身
- 2026-08-16 P0-C 完成:砍通长分割线,dock 改浮动卡片(栏间缝 + 圆角 14px),修掉 dock 双标题头
- 2026-08-16 P0-D 完成:去示范数据(项目组空态/画像假数值→诚实空态,hasDims 判定),聊天列 max-w-3xl 居中 + 输入条浮动圆角,dock 默认宽 340→320
- 2026-08-16 P1-A 完成:双槽位骨架落地 — 新建 `lib/surfaceRegistry.tsx`(表面注册表:label/title/icon/component/mainRoute,无 mainRoute 表面不可交换)、`lib/layoutStore.ts`(配对记忆 zustand,localStorage `dm_layout_pairing`)、`hooks/useChatSend.ts`、`components/dock/ChatSideSurface.tsx`(副槽迷你对话,共享 chatStore);改写 RightDock(切换器+交换⇄+联动/固定)/CenterDock/dockTabs/uiStore/ChatPage;修正旧 ROUTE_DOCK_MAP `/task-planning`→`/tasks` 死映射;登记 B8/B9;tsc+build 全绿,截图 real_p1a_{switcher,chat_side,swapped}.png
- 2026-08-16 P1-B 完成:上下文工作台 — ContextDockContent 重写(本轮注入条:域 token 占比分段条+图例+意图域;记忆卡三态:正常/钉住 amber 边+实心 Pin/移除 45% 透明+Undo2,汇总条+全部恢复;8s 静默轮询;保留 GAP-4 压缩反馈);新建 `stores/contextWorkbenchStore.ts`(djb2 内容指纹键,in-memory 不持久化);surfaceRegistry:/chat 默认配对 profile→context,标题改「上下文工作台」;登记 B10;tsc+build 全绿,截图 real_p1b_{workbench,marks,empty}.png
- 2026-08-16 P1-C 完成:顶栏万能搜索栏 + 工作台模式 + placement 归坞头 — ① Toolbar 重写:装饰性搜索框改 ✨触发器(搜索·提问·代操作+⌘K),画像监控降为状态点(绿/灰/红,15s 轮询,点击→/profile);② 新 OmniboxPalette:13 页面+2 操作真实可用,⌘K 全局开合,↑↓/Enter/Esc,内容搜索(B13)/元认知代操作(B14)留位;③ 上下文工作台模式切换:列表 / 图结构(B11) / 精调(B12),后两者 ModePending 诚实占位;④ 悬浮/嵌入挪入坞头(副槽⇄浮层互转),DockPicker 退出侧栏(文件现为无引用死代码,与 RightPanel.tsx 同批待清理);⑤ 修 Esc 穿透 bug:SidePanel/CenterDock 的 window 级 Esc 监听曾把 palette 背后的坞一起关掉,palette 内 stopPropagation;登记 B11–B14;tsc+build 全绿,截图 real_p1c_{toolbar,omnibox,mode_graph,mode_finetune,float}.png
- 2026-08-16 P1-D 完成:比例对齐 mockup v2 — 侧栏 228px/nav 13px 纵向 6px/图标 15px/新会话药丸;顶栏 52px/标题 15px/触发器药丸 12px/幽灵钮 30px;聊天列 720px/输入条全药丸/发送钮 32px/输入文本 13.5px;副槽切换器药丸(wash 底)/卡片 rounded-card 10px/钉住改 inset 2px 左条;FAB 56→40px 中性化;新增 `--bg-wash`/`--bg-wash-strong` 双主题 token(修复:light.css 的 card-hover 覆盖选择器会把同时带 hover: 类的元素误排除,导致浅色下切换器变深丸);tsc+build 全绿,截图 real_p1d_{dark,light}.png
- 2026-08-16 P1-E 完成:质感基调 — ① 新建 dark.css(light.css 镜像):暗色下 Tailwind 静态 gray 阶(#E5E7EB 等亮灰)是"白线分隔"根因,全局重映射 border-gray-50~300/700/800 + divide-gray-100/200 → `--border-hairline`,bg-gray-50/100→`--bg-wash`、bg-gray-200→`--bg-wash-strong`,hover 底色同步;描边重映射带 `:not(:focus)` 守卫保留焦点描边,hover:border-gray-200→border-medium 防闪白;② DockContents 全部信息卡去描边走 wash 灰度阶梯(钉住 inset 2px 左条保留,按钮/输入框描边保留);③ 浮层玻璃化:omnibox/RightDock 下拉/CenterDock 悬浮坞 → 新 token `--bg-glass`(双主题)+ backdrop-blur-xl + backdrop-saturate-150 + `--shadow-float`;④ Toolbar 触发器:「搜索 · 提问 · 代操作」→「搜索」,⌘K→Ctrl K;⑤ Sidebar 激活/hover 改 wash,新会话 bg-primary/10 描边钮 → 实心 bg-primary 白字(mockup .btn-primary);tsc+build 全绿,截图 real_p1e_*.png;无新增后端需求(沿用 B11–B14)
- 2026-08-16 P2 完成:项目组接数据 — ① 新 `stores/projectStore.ts`(zustand persist,`dm_projects`):projects(id/name/color/createdAt,六色板循环)+ sessionProject(sessionName→projectId)+ activeProjectId(过滤范围,null=全部);② Sidebar:项目组「+」内联输入建项目(Enter 建并激活跳 /sessions,Esc/失焦取消),项目点击切激活(再点取消),删除死代码 ProjectItem/空 projects;③ SessionsPage:列表头过滤 chip(色点+名称+计数+× 清除)/「项目下暂无会话」空态/行内归入按钮(常显 FolderPlus,玻璃下拉选项目或移出)/已归属行 KB 前色点;行容器 motion.button→motion.div(role=button)使嵌套按钮合法;④ Toolbar 触发器白线修复:bg-surface-card+border-subtle → bg-wash+border-hairline(kbd 同步);⑤ 测试插曲:getByRole 名称匹配会把行容器(内容含"归入项目")排在真按钮前,改用 aria-label 属性选择器;归入按钮由 hover 显现改常显(触屏无 hover);登记 B15/B16;tsc+build 全绿,截图 real_p2_{toolbar,empty,dropdown,filtered}.png
- 2026-08-16 P2b 完成:项目管理操作 — ① projectStore 补 `recolorProject`,色板导出为 `PROJECT_PALETTE`;② Sidebar 项目项:右键 或 hover 显现的「⋯」开玻璃菜单(重命名 → 内联输入 Enter/Esc;六色改色板;删除项目红钮,归属映射随 deleteProject 自动清除、激活态自动复位),菜单带 click-away 遮罩;③ 行首字徽随重命名自动更新,会话页过滤 chip 同步;无新增后端需求(沿用 B15/B16);tsc+build 全绿,截图 real_p2_{menu,rename,renamed}.png
- 2026-08-16 P1-F 完成:白线根因根治 + 立体感 + 滑动药丸 — ① 核查确认"白线"真根因是命名缺口:tailwind.config 的 `colors.border.{subtle,medium,hairline}` 嵌套结构只生成 `border-border-*` 工具类,全站 60 文件 201 处的 `border-subtle` 等从未被生成,边框色回落 preflight 默认 #E5E7EB;在 index.css 末尾 `@layer utilities` 补 `.border-subtle/.border-medium/.border-hairline` 三个真工具类(颜色走双主题 CSS 变量;同层内 hover:/focus: 变体带伪类特异性更高,交互描边安全),构建产物验证 `.border-subtle{` 已存在;② 立体感:dark.css 给 shadow-card 加 `inset 0 1px 0 rgba(255,255,255,0.045)` 顶部内高光(苹果式上方打光),body 加 1100px 径向环境光(rgba 白 2.8%,顶部 -8% 偏移);③ Sidebar 滑动 hover 药丸:共享 `layoutId="nav-hover-pill"` 的 motion.span 在页面项与项目项之间弹簧滑动(500/40),替换原 `hover:bg-wash` 瞬时变色,nav onMouseLeave 清空,激活项保持静态 wash 不叠药丸;tsc+build 全绿,截图 real_p1f_{dashboard,hoverpill,sessions}.png;无新增后端需求
- 2026-08-16 P3 完成:会话切换器 + 路由驱动会话 — ① chatStore 加 `loadSession(id,msgs)`;② 路由 `chat` → `chat/:sessionId?`;③ ChatPage 路由驱动:`/chat`=恢复或新建、`/chat/new`=强制新会话(建后 replace 回 /chat)、`/chat/:id`=getHistory(`/v3/session/{id}/history`,HistoryRecord{message_id,role,content,timestamp ISO,intent,latency_ms} → ChatMessage)真载入历史,失败降级空会话允许进入,effect deps 有意仅 [routeId];④ 修 `/chat/default` 死链(原命中 NotFound):Sidebar 新会话→/chat/new、MobileBottomNav 聊天→/chat、SessionsPage 新建 Session→/chat/new、DashboardPage 新建会话主按钮→/chat/new(同文件两处 navigate('/chat') 用后续 className 锚点区分);⑤ 新 `components/SessionSwitcher.tsx`(props {fallbackTitle}):过滤框 +「+ 新会话」+ 按 projectStore 分组(项目色点/计数/当前项琥珀高亮✓)+ 未分配组,玻璃浮层,Esc/点外关闭,打开时 refresh;Toolbar 保留 sessionTitle 透传 fallbackTitle,ChevronDown import 移除(noUnusedLocals);v6 会话名为裸 id 无 .json 后缀,stripExt 防御保留;tsc+build 全绿,截图 real_p3_{switcher,switched}.png;无新增后端需求 — 切换用现有 getHistory,B5 会话标题摘要仍待后端
- 2026-08-16 FAB 归档:FloatingActionButton + ChatOverlay(仅被 FAB 引用)→ `components/un_use/`(git mv 留历史,不删,后续可复用);App.tsx 摘除 import 与挂载;overlayStore 保留(ContentScriptBridge 仍在用);依据:FAB 是第四对话入口,与主聊天/副槽对话/万能搜索栏重复;tsc+build 全绿,截图 real_fab_removed.png;无后端影响
- 2026-08-16 P1-G 完成:全局玻璃质感(macOS 式) — ① 新 token `--bg-glass-strong`(dark rgba(23,19,33,.86)/light rgba(248,246,241,.86),面板级,不透明度高于 --bg-glass 的 .72 以保文字可读)接入 tailwind `bg-glass-strong`;② 顶栏玻璃浮层:Layout 重构 — Toolbar 从 in-flow 挪为 main 内 `absolute top-0 inset-x-0 z-30` 浮层,滚动容器(motion.div)加 `pt-[52px]`,全页面内容滚动时从玻璃顶栏下穿过;浮层仅覆中栏宽度,右坞保持全高浮卡(macOS 检查器式);header 本体 bg-surface → bg-glass + backdrop-blur-xl + saturate-150 + border-b hairline;③ 聊天输入条玻璃浮条:ChatPanel 根加 relative,消息滚动区 `py-3` → `pt-3 pb-28`,ChatInput 包入 `absolute bottom-0 inset-x-0` 玻璃横条(bg-glass + blur-xl + saturate-150 + border-t hairline),消息从其下滚过(Apple Messages 式);④ 左侧栏 `bg-surface-sidebar` → `bg-glass-strong backdrop-blur-xl backdrop-saturate-150 border-r border-hairline`(桌面端透出 body 径向环境光,移动抽屉真模糊);⑤ 右坞浮卡 `bg-surface-dock` → glass-strong 同款;设计原则记录:backdrop-blur 只在浮于滚动内容之上的层有视觉效果,内联按钮保持实色 wash 以拉开"基底平直/浮层玻璃"层级;tsc+build 全绿,截图 real_p1g_{chat,dashboard}.png;无新增后端需求;浅色主题下 P1-F/P1-G 新增面未截图验证(均走双主题 token,理论安全)
- 2026-08-16 P1-H 完成:液体玻璃 + scrim 根治 + 侧栏拖拽 + 分隔光影 — ① scrim 根因根治:light.css `[class*="bg-surface"]` 通配规则(带双 :not,特异性 0,4,1)把遮罩的 `bg-surface-overlay` 刷成不透明 #FDFCF8,第 20 行的 0.04 修复因特异性低从未生效 — 亮色开搜索全白即此;新建独立 token `--bg-scrim`(dark rgba(6,5,10,.55)/light rgba(28,28,26,.18))+ tailwind `bg-scrim`,类名不含 "bg-surface" 彻底绕开通配;OmniboxPalette/Modal(bg-black/50)/SessionDetailDrawer 三处遮罩全换;② 液体玻璃材质 `.glass-panel`(@layer components):padding-box/border-box 双背景层实现渐变 rim light(165deg,顶亮底暗)+ `inset 0 1px 0` 顶部内高光 + `inset 0 -10px 18px -10px` 底部内阴影(厚度感),全部走双主题 token(--glass-rim-*/--glass-hi/--glass-losh);omnibox/会话切换器/侧栏项目菜单/会话页归入下拉/右坞下拉五处浮层统一替换;真折射(SVG backdrop filter/shader)评估后明确不做(脆且贵);③ 侧栏指针镜面高光:`.sidebar-spec::before` 径向渐变(180px)跟随 onPointerMove 写入的 --mx/--my,hover 0.35s 淡入,liquid glass specular 的 CSS 近似;④ 左侧栏可拖拽调宽:uiStore `dm_sidebar_width` 持久化(180-320,默认 228),style width 驱动(collapsed 时回退 w-16),拖拽中 transition-none 防跟随迟滞,手柄复用右坞 hover 显条样式,桌面端专属;右缘改 `.edge-fade-r` 渐变发丝线(顶部受光强→向下渐隐);⑤ 结构条软投影:`.shadow-bar-b`(顶栏)/`.shadow-bar-t`(输入条),分隔立体感来自光影而非更粗的线;CenterDock 浮坞保持原样未换(用户称其现有效果已有美感);tsc+build 全绿,截图 real_p1h_{omnibox_dark,omnibox_light,sidebar_spec}.png;无新增后端需求;用户方向备忘:液体玻璃后续可推广到页面级卡片,内容呈现关系后续再调(先骨架后调)
- 2026-08-16 P1-I 完成:高光约束进按钮 + 剩余玻璃面全换装 — ① 用户反馈整栏大光晕越界,废弃 `.sidebar-spec`,改 `.spec-item` 逐按钮 spotlight(spotlight card 模式:item overflow 裁剪圆角,::before 110px 径向渐变跟 item 本地 --mx/--my,hover 0.25s 淡入;--spec-color 暗色 0.07→0.10 补偿面积缩小),NavLink 与项目项 button 各接 onPointerMove={onSpecMove},aside 上的整栏 handler 移除;性能论证:每帧仅写 2 个 CSS 变量触发小面积重绘,无 layout;未选 transform 高光层方案(每按钮多一个 DOM 节点);② `.glass-panel-strong` 变体(--bg-glass-strong 基底 + 同款 rim/内高光/内阴影),右坞浮卡换装;CenterDock 浮坞换 `.glass-panel`(保留 border-l hairline 因贴容器三边);③ `.shadow-bar-b/t` 叠加 `inset 0 1px 0 var(--glass-hi)` 顶部内高光,结构条统一上方打光;调研结论(LogRocket/freefrontend):真液体折射=SVG feDisplacementMap+feSpecularLighting,性能/可访问性成本高,业界建议仅高曝光小组件 — 维持不做;tsc+build 全绿,截图 real_p1i_{spec_item,dashboard,light}.png(浅色一并验证);无新增后端需求
- 2026-08-16 P1-J 完成:药丸滑动回归修复 + 浮层追光 + 页面卡片液体化试点 — ① 回归根因:P1-I 的 `.spec-item{overflow:hidden}` 会裁掉 layoutId 共享药丸的飞行过程(framer 用 transform 让药丸从旧位置飞入,经过新父级裁剪区时不可见 → 用户看到"冒出来"而非滑动);修复 = 移除 overflow,改 `::before{border-radius:inherit}` 让辉光自身裁圆角,飞行中间帧截图(real_p1j_pill_flight.png)实证药丸恢复可见;② 新 `lib/spec.ts` 导出共享 `specMove` handler,`.spec-panel` 浮层追光(320px 大半径 + --spec-panel-color 低透明 sheen,border-radius inherit)接入 omnibox/会话切换器/右坞下拉/会话页归入下拉/侧栏项目菜单五处;③ `.card-liquid` 页面级液体卡片(@layer components:--bg-card-liquid 半透明基底 padding-box + 165deg 渐变 rim 62% 渐隐 border-box,无 backdrop-filter — 页面卡随内容滚动背后是静态底,省模糊开销),总览页 6 张卡(统计卡×2/图表卡×3/列表卡×1)试点,沿用 shadow-card;tsc+build 全绿,截图 real_p1j_{pill_flight,pill_landed,cards_dark,omnibox_sheen,cards_light}.png;无新增后端需求
- 2026-08-16 P1-K 完成:亮色光圈调色 + 页面粘边修复 + 设计语言落档 — ① 用户反馈亮色下鼠标光圈无感:调研确认亮色玻璃的质感来自色彩 tint/边缘近白 rim/柔和阴影而非白光斑,亮色 --spec-color/--spec-panel-color 由白(0.55)换品牌琥珀 rgba(217,119,6,0.14/0.08),侧栏按钮与 omnibox sheen 亮色下可见(截图实证);② 页面粘边:SessionsPage/SettingsPage 根容器 `max-w-5xl mx-auto` 但零横向 padding,主区不足 1024px 时内容贴死左右边界 — 加 `px-6 lg:px-10 pb-10`;③ 设计语言沉淀为新文档 `docs/only/frontend/DESIGN.md`:五条核心原则/材质层级表(glass-panel·glass-panel-strong·card-liquid·结构条·spec-item·spec-panel·bg-scrim)/双主题规则/交互规范/禁区教训(overflow 裁 layoutId·通配符词根·colors 嵌套不生成类·拖拽禁过渡)/文件地图/后续方向;tsc+build 全绿,截图 real_p1k_{spec_light,omnibox_light,sessions}.png;无新增后端需求
- 2026-08-16 P1-L 完成:右坞切换器滑动药丸 + 侧栏药丸提速 + card-liquid 全站铺开 — ① 用户拍板"右边辅助栏点击切换效果挺好,加上左边的平滑移动变化就完美,平滑移动稍微快一点跟进鼠标":右坞表面切换器下拉加 layoutId="dock-switcher-pill" 滑动药丸(新 state switcherHover,容器 onMouseLeave 清空,选项 onMouseEnter+relative+去瞬时 hover 底色,spring stiffness 700/damping 45 比侧栏更敏捷);侧栏药丸提速 500/40→680/45(页面项+项目项两处);② card-liquid 全站铺开 9 页 60 张卡:CognitiveProfile×3(border-border-subtle)/Behavior×5(xl4+lg1)/DeepChain×7(xl3+lg4)/Engineering×4/Gateway×16/MetaCenter×15/Graph×1(details 卡)/Sessions×1/Settings×8,纯字符串 replace 六条规则长模式优先防误伤,Graph 页 rounded-md 分段控件有意保留;tsc+build 全绿,截图 real_p1l_{switcher_flight,switcher,engineering,meta}.png;无新增后端需求
- 2026-08-16 P1-M 完成:card-liquid 指针追光 + 浅色铺开页抽查 — ① 用户提出"做了玻璃效果的范围内鼠标发光可让玻璃质感更好,中间玻璃块偏大是否按大小调光圈":采用 ellipse 42% 42% 百分比半径(相对卡片宽高),一条规则实现大卡大光晕/小卡收敛,无需尺寸分档;card-liquid::before 承载追光(叠于内容之下,半透明基底透光不遮字),--card-spec-color 双主题(暗 0.06 白/亮 0.10 琥珀),card-liquid 补 position:relative 定位上下文;② main.tsx 全局 pointermove 委托:单监听器 closest('.card-liquid') 写 --mx/--my,passive,零 JSX 侵入覆盖现有 60 张卡及未来卡片,与 spec-item/spec-panel 手挂 specMove 共存无冲突;③ 浅色铺开页抽查收口:元认知/工程/网关浅色验证通过无白线暴雷,琥珀追光在浅底呈现温暖光泽;tsc+build 全绿,截图 p1m_{meta_spec_small_dark,meta_spec_big_dark,meta_spec_light,engineering_light,gateway_light}.png;无新增后端需求
- 2026-08-16 P1-N 完成:边缘 rim 追光(玻璃感修正) — ① 用户指出 P1-M ellipse 大光晕"完全没有玻璃感: 之前卡小光圈很快到达边缘, 边缘感被加强才有质感, 现在光到不了边缘": 调研确认业界共识(Win10 Fluent 边框照亮/Cruip spotlight card p-px 缝透光/antd BorderBeam mask-composite 环带/Aceternity moving border) — 玻璃追光的主角是边缘不是面板;② card-liquid::after 新增 rim 追光: padding 1.5px 撑环带 + mask-composite: exclude(-webkit- xor 兜底)差集裁出边缘环, radial-gradient 240px circle at --mx/--my, 鼠标近哪段边缘哪段亮, --card-rim-glow 双主题(暗 rgba(255,255,255,0.50)/亮 rgba(217,119,6,0.45));③ ::before 中央光降级辅助: ellipse 42% → 280px circle 固定光斑, --card-spec-color 减淡(暗 0.04/亮 0.07)防雾面; 列表场景中光斑近似"整行照亮"效果自然;tsc+build 全绿,截图 p1n_{meta_center_dark,meta_edge_dark,meta_edge_light,sessions_zoom_dark}.png(meta 大卡贴边 rim 亮段清晰,浅色琥珀 rim 成立);无新增后端需求
- 2026-08-16 P1-O 完成:rim 边缘追光风格统一 + 会话/设置页顶距 — ① 用户确认 P1-N 方向("可以,风格统一一下,之前的那些也加入边缘发光"): spec-item::after 加 rim 环带(130px 光斑,小按钮边缘几乎全程点亮,侧栏 hover 整圈描边,正是用户怀念的"光到达边缘"质感), spec-panel::after 加 rim 环带(320px,omnibox/切换器下拉边缘流光), 复用 --card-rim-glow token, mask-composite exclude + -webkit- xor 双写兜底;② 用户反馈"会话页面顶有点太高/距离顶上太接近": SessionsPage 根容器 `px-6 lg:px-10 pb-10` 缺 pt(P1-K 修粘边只补了横向), 补 pt-6 对齐 Dashboard py-6 基准; SettingsPage 同款同步; ChatPage(h-full 全高消息流)布局使然不动;③ DESIGN.md 升级 v2: 材质表 spec-item/spec-panel 双层配方、禁区新增第 5 条(mask 环带双写兜底 + 1.5px 细带色必须高透明度)、文件地图补 main.tsx 全局委托与参考实现更新、后续方向划掉已完成项(card-liquid 铺开/双层追光/rim 统一/顶距);tsc+build 全绿,截图 p1o_{sidebar_rim_dark,omnibox_rim_dark,sessions_top_dark}.png;无新增后端需求
