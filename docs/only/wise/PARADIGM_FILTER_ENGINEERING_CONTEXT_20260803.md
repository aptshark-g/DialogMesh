# 哲学消解 — 工程链 + 上下文待讨论项过滤（2026-08-03）

> 方法: 用 `PARADIGM.md`（A1-A25 公理 + P1-P28 原则 + §6 伪问题清单）
> 过滤 `STATE_HANDOFF_ENGINEERING_CONTEXT_20260803.md` §六 的 17 项待讨论点。
> 结论: **17 项 → 12 项消解为施工/清理/修复项，5 项保留为核心讨论**。

---

## 一、被哲学消解的 12 项（→ 施工清单，不再拍板）

| 原项 | 消解依据 | 消解为 |
|---|---|---|
| A1 工程链定位（收缩 vs 补全）| A14: 工程链 = 不变量链（约束从事实归纳，非声明）；A12: 约束空间。方向已定，非选择题 | 施工范围与顺序 |
| A2 v3_2 shim 缺 models.py | P22 白盒: 旧路径要么修要么明确废弃 | 技术债清理 |
| A3 persistence_full 重复文件 | — | 清理项 |
| A4 `__init__` Artifact 导出陷阱 | P22 白盒: CLI/API 面必须正确 | P0 修复 |
| A5 白盒 CLI（UUID vs name）| P22 白盒化是承诺 | P0 修复 |
| A7 api_doc_preprocessor 6 组件 | P20/P23 竞争吸收 + 代价排序 | 施工项（不阻塞）|
| A8 工程链持久化 | A2 颗粒度 + A15 温度映射 UnifiedGraphStore | 施工项 |
| B2 assembly 5 处 API 错配 | A1 模块职责固定: 两处都按契约修 | 接线 bug 修复 |
| B3 两套矩阵/枚举归一 | 代码分裂（同一设计两处定义），非设计分歧 | 归一施工 |
| B4 to_prompt 0-budget 语义 | A15/A16: 预算耗尽 → 降级表示（摘要/索引），不是全量或全空 | 实现修复 |
| B6 锁创建时机 | — | 施工项 |
| C1 中文分词统一 | A25: BM25 是第一级粗筛；复用已有 stanza/jieba | 施工项 |
| C2 状态文档纠偏 | A18: 验证必须真实（假状态 = 违反公约）| 立即修正 |
| C4 测试策略 | A18: 黄金示例集 + 真实断言是公约义务 | 施工项 |

---

## 二、保留的 5 项核心讨论（按优先级）

### C1. 上下文 × 子图协同边界（核心中的核心）
- 哲学锚: A3 关系第一公民 + A8 表达形式哲学 + A25 召回重建上下文。
- 问题: Context 决定「看什么/多少/优先级」，子图决定「以什么结构呈现」。
  CONTEXT phase 补全后，ContextAssembler/IR 与 SubgraphCompiler 的分工边界在哪？
  表达形式（XML/JSON/NL，A8）由谁决策、如何落到 IR 输出？

### C2. Event ID 跨域索引（上下文工程的内核基建）
- 哲学锚: A3（关系是 first-class）+ A25（召回 = 嵌套证据组装，需要 JOIN 键）。
- 问题: 设计 §3「Event ID = 跨域 JOIN 键」从未实现。如何接——
  复用关联链 Event Sourcing 基建（A20 竞争吸收 + 代价评估）还是独立实现？

### C3. 约束空间的工具域 + 上下文快通道
- 哲学锚: A21（没有运行时后果的约束是假约束）+ A16（快反馈后修正）。
- 问题: 工程链约束是否纳入工具/命令域（MCP 校验）？
  上下文快通道（CLI 直接 LLM）给什么、后补修什么？

### C4. 代码分裂治理（5 套并行实现 + v3 会话层去留）
- 哲学锚: P5 冻结知识复用 + A16 不阻断 + A15 温度分层。
- 问题: `context_manager/`（87KB）与 `context/` 是替代关系还是各有生态位？
  v3 ContextManager（47 测试绿）作为基础设施保留复用，还是随 v6 迁移升级？

### C5. 工程链施工范围与顺序
- 哲学锚: A14 已定方向（约束推理引擎是公约义务），只剩范围/顺序。
- 问题: 首期做到哪一档（模型层 → 推理层 → 接入层）？先修 P0 还是先接子图 K 域？

---

## 三、讨论顺序建议

1. **C1 上下文×子图**（决定上下文工程的最终形态）
2. **C2 Event ID 索引**（决定跨域关联的实现路径）
3. **C4 代码分裂治理**（决定施工时的收敛目标）
4. **C3 约束工具域 + 快通道**（决定工程链接入面）
5. **C5 施工范围**（其余四点的落点）
